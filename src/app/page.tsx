export default function Home() {
  return (
    <main style={{ minHeight: "100vh", display: "grid", placeItems: "center" }}>
      <div style={{ textAlign: "center" }}>
        <h1 style={{ fontSize: "2rem", fontWeight: 700 }}>Make Store</h1>
        <p>CI + CD via GitHub Actions (Next 16 + GitHub Pages)</p>
      </div>
    </main>
  );
}
